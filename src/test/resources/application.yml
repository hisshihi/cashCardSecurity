spring:
  #  datasource:
  #    url: jdbc:h2://mem:db;DB_CLOSE_DELAY=-1
  #    driver-class-name: org.h2.Driver
  #    name: sa
  #    password: sa
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true